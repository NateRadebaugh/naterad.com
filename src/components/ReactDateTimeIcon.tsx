import Icon from "./Icon";
import { faCalendarAlt } from "@fortawesome/free-solid-svg-icons";

function ReactDateTimeIcon({ size }) {
  return <Icon size={size} icon={faCalendarAlt} />;
}

export default ReactDateTimeIcon;
