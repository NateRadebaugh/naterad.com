import Icon from "./Icon";
import { faTwitter } from "@fortawesome/free-brands-svg-icons";

function TwitterIcon({ size }) {
  return <Icon size={size} icon={faTwitter} />;
}

export default TwitterIcon;
