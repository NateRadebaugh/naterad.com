import Icon from "./Icon";
import { faGithub } from "@fortawesome/free-brands-svg-icons";

function GitHubIcon({ size }) {
  return <Icon size={size} icon={faGithub} />;
}

export default GitHubIcon;
