import Icon from "./Icon";
import { faFileAlt } from "@fortawesome/free-solid-svg-icons";

function ResumeIcon({ size }) {
  return <Icon size={size} icon={faFileAlt} />;
}

export default ResumeIcon;
